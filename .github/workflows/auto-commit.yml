name: Auto commit change

on:
  pull-request:
    branchs:
      - main

jobs:
  auto-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          persist-credentials: false

      - name: Configure Git
        run: |
          git config user.name 'tresor-del'
          git config user.email 'tresoradzanto7@gmail.com'

      - name: Run tests
        run: pytest
